# Per-project functions params (committed on purpose — a uid is not a secret).
# Without this file, `firebase deploy --only functions` and the emulator PROMPT
# for every unset param (the emulator hangs silently in non-interactive runs).
# The owner's uid — where new-signup access requests are pushed (BB-210).
ACCESS_ADMIN_UID=sOG6SB6tazOUPWCgC4i7UciYXHh1
